# BRACE v2:从数学推演倒推实验设计

> 写作动机:Line A pilot 的三个结果(B1 相对 N1 在 preservation 和 easy adaptation 上均 5/5
> 更差、anchor 仅 +0.03 且 3/5 同向、B2/B3 feasibility 10/10 fail)不是三次独立的坏运气,
> 而是三组可以在开跑前用算术算出来的必然结果。本文用当前代码里的真实常数完成这些算术,
> 给出五条可检验预测(P1–P5),再从推演反推 v2 的每一个设计参数。
> 风格参照 RoPE:先写下想要的数学性质,再解出满足它的设计,而不是先设计再看实验能不能过。

已核实的实验常数(全部来自当前分支代码与归档,非估计值):

| 常数 | 值 | 来源 |
|---|---|---|
| 专家数据帧数 N | 31,645(200 episodes) | `brace_dataset_manifest.json` |
| B1/N1 chunk 数 | 各 5 个,每个 horizon=8 帧,共 M=40 帧 | `place_base200_v2_{B1,N1}.jsonl` + `build_screen_dataset.py` |
| batch 构成 | 128 = 112 expert + 16 chunk(`ROLLOUT_PER_BATCH=16`) | `_schedule_place_base200_v2_pilot.py` |
| 训练量 | 247 步/epoch × 10 epoch = T≈2,470 步 | 同上 |
| 对偶参数 | λ₀=0,η_λ=0.01,ε=10⁻⁴(identity 冒烟容差) | `robot_dp_14.yaml` |
| 锚定/SFT 梯度比 | 0.018 | A1 校准 `summary.json` |
| anchor replay set | ~16 个固定窗口(2 组×8 条×1 窗口) | `build_anchor_replay_set.py` |
| 每点验证续跑 | candidate 3 次 vs control 9 次 | branch 归档 |
| traced corpus | 100 env seeds:32 mixed / 52 全成功 / 16 全失败 | 云端核验 |

---

## 0. 记号

- MDP:状态 s,动作片段(chunk)a ∈ R^{6×14},回报为终局成功指示 G ∈ {0,1}。
- 基础策略 π₀(diffusion policy,θ₀),部署用 EMA 权重。
- chunk 级值函数与优势:
  - V^{π₀}(s) = E[G | s, 之后全程 π₀]
  - Q^{π₀}(s, a) = E[G | s, 执行片段 a, 之后全程 π₀]
  - **A^{π₀}(s, a) = Q^{π₀}(s, a) − V^{π₀}(s)** ← 这是 branch verification 唯一应该估计的量
- SFT 混合损失:L(θ) = (1−ρ)·E_{expert}[ℓ] + ρ·E_{chunk}[ℓ],实际 ρ = 16/128 = 1/8。
- 锚定约束:d_g(θ) = E_{x∼A_g}‖f_θ(x) − f_{θ₀}(x)‖²,要求 d_g(θ) ≤ ε。

---

## 1. 推演 I:当前"验证"估计的根本不是优势函数

### 1.1 对照组的混淆项分解

当前 control 是把**失败轨迹在状态 s' 生成的动作 a_fail** 移植到成功轨迹状态 s 上执行。
于是实际估计的量是:

```
Δ_measured = Q^{π₀}(s, a_succ) − Q^{π₀}(s, a_fail|s'),   其中 a_fail ∼ π₀(·|s'), s' ≠ s
```

把它与真正想要的优势相减,得到混淆项:

```
Δ_measured = A^{π₀}(s, a_succ)                    ← 想要的信号
           + [V^{π₀}(s) − Q^{π₀}(s, a_fail|s')]   ← 混淆项:状态失配惩罚
```

混淆项恒 ≥ 0 的期望(错位动作至多与按状态重采样一样好),且分支点选择规则**最大化
成功/失败轨迹的关节距离**,即主动最大化 ‖s − s'‖,从而系统性放大混淆项。
所以 `advantage=1.0, control=0/9` 这类完美结果无法区分两种解释:

- (i) a_succ 真的好;
- (ii) a_fail 在错误状态上执行必然崩,而 a_succ 只是"正常"。

**正确对照**:control 动作从**同一状态 s** 由冻结 π₀ 换 diffusion 噪声重采样。此时
Δ = Q(s, a_succ) − E_{a∼π₀(·|s)}Q(s, a) = A^{π₀}(s, a_succ),混淆项恒等于零。

### 1.2 统计功效:K=3 对 9 在配对精确检验下几乎无功效

对 candidate a/K₁ vs control b/K₂ 做单侧 Fisher 精确检验(已精确计算):

| 设计 | 结果 | 单侧 p |
|---|---|---|
| 现行 K=3 vs 9 | 2/3 vs 1/9 | 0.127 |
| 现行 K=3 vs 9 | 3/3 vs 7/9 | 0.545 |
| 现行 K=3 vs 9(最好情形) | 3/3 vs 0/9 | 0.0045 |
| v2 建议 K=8 vs 8 | 7/8 vs 1/8 | 0.0051 |
| v2 建议 K=8 vs 8 | 6/8 vs 1/8 | 0.0203 |
| v2 建议 K=8 vs 8 | 6/8 vs 2/8 | 0.0660 |

现行设计只有"candidate 全成 + control 全败"一种格局能显著——所以 5 个 accepted 里
只有 2 个真正有证据,这与 A/B 两份审查的判断一致。另外 bootstrap-LCB 在 3/3 时
退化为 1.0(重抽样只能抽到成功),`lcb_advantage=1.0` 的大量出现是这个退化的直接产物。

**v2 规格由此解出**:m = 32 mixed seeds × 3 点 = 96 个检验;想在 BH(FDR=0.1)下拿到
~30 个发现,单点 p 门槛约 0.1×30/96 ≈ 0.031;查上表得 **K=8 对 8、公共续跑种子配对、
seed 级聚类**是满足该门槛的最小设计(6/8 vs 1/8 即可通过)。总计 96×16 = 1,536 条续跑。

---

## 2. 推演 II:B1 五连败是"过采样 × 残差 × 干扰"三因子乘积,可事前算出

### 2.1 过采样因子:w ≈ 99

chunk 数据的自然占比 q = M/(N+M) = 40/31,685 ≈ 0.00126,而混合权重 ρ = 1/8,
故有效重要性权重

```
w = ρ/q ≈ 99
```

换算成访问次数:T=2,470 步内,每个 chunk 窗口被梯度访问 16×2,470/5 ≈ **7,904 次**,
每个专家帧只被访问 112×2,470/31,645 ≈ **8.7 次**——**约 900 倍的逐样本访问比**。
5 个 6 步窗口以 900 倍强度反复灌入网络,这不是"数据混合",是**带权记忆化**。

### 2.2 残差因子:B1 的选择规则恰好选中残差最大的样本

SFT 每步对 chunk 窗口的拉力 ∝ 残差 ‖a_target − f_θ(s)‖。关键区别:

- **N1** chunk 是成功轨迹在**任意匹配位置**的动作——π₀ 自己的典型样本,
  接近 f_{θ₀}(s) 的条件均值,残差小 → 900 倍权重乘以近零残差,漂移仍小。
  这解释了 N1 ≈ U0。
- **B1** chunk 按**成功/失败分歧峰值**选点。分歧大 ⟺ π₀ 在该状态的动作分布方差大
  (同一状态采样出成败两支)。从高方差分布中取单个样本做 MSE 目标,残差按分布方差
  下界:E‖a_sample − f_θ(s)‖² ≥ Var[π₀(·|s)],而选点规则**最大化**这个方差。
  → 900 倍权重乘以被最大化的残差 = 最大化的参数漂移。

### 2.3 干扰因子:漂移不留在分支状态局部

一阶(NTK)近似下,对 chunk 状态 s_c 的过采样更新对任意其他状态 s 的函数改变为

```
Δf(s) ≈ −η_lr · w · K(s, s_c) · r(s_c)      (K 为切核,r 为残差)
```

网络平滑性使 K(s, s_c) 在观测相似的状态上不为零,于是"对 5 个状态的 900 倍记忆化"
以核加权的方式污染全空间——同时打击 preservation(旧状态)和 adaptation
(easy confirm 状态)。**这正是观测到的 B1 双线 5/5 全败,且幅度(−0.12∼−0.29)
远超 5 个 chunk 所占数据比例能解释的水平。**

### 2.4 被违反的理论约束:CPI 混合率

保守策略迭代(Kakade–Langford)给出:用混合率 β 把"分支动作策略"混入 π₀,

```
J(π_new) − J(π₀) ≥ β·Ā/(1−γ) − 2γβ²·ε_max/(1−γ)²
```

改进只在 β 足够小时有保证;β 的量级应由验证优势 Ā 与最坏干扰 ε_max 之比解出,
而不是拍脑袋定 ρ=1/8。当前 w≈99 等价于在分支状态邻域取了接近 1 的局部 β——
处于该界的惩罚项主导区,理论上就应当变差。

### 2.5 v2 数据构造规格(由 2.1–2.4 解出)

要求 **w = ρ/q ≤ 3**。两条路同时走:

1. **增大 M(首选)**:导出**整条已验证的成功续跑**(每条 ~85–98 转移),而不是只导出
   6 步 chunk。32 mixed seeds、按 20–40% 接受率、每点 8 条续跑中的成功者,可得
   **~6,000–12,000 帧**已验证 on-policy 数据(当前是 40 帧,提升 150–300 倍)。
   注意:续跑尾部是 π₀ 自己的行为,监督它相当于温和的 on-policy 蒸馏,残差小,安全。
2. **压低 ρ**:若最终 M≈8,000,则 q≈0.20,取 ρ≈0.25–0.5 即得 w≈1.3–2.5,落入安全区。
   在 M 补足之前,任何 ρ 都不许超过 3q。

---

## 3. 推演 III:对偶锚定的失败在时间尺度上差了三个数量级

### 3.1 λ 的可达上界

dual-only 更新 λ_{k+1} = [λ_k + η_λ(d_g − ε)]₊,λ₀=0。以归档中典型违反量
d−ε ≈ 10⁻³ 计:

```
λ_T ≤ η_λ · T · (d−ε) ≈ 0.01 × 2,470 × 10⁻³ ≈ 0.025
```

### 3.2 需要的 λ*

A1 校准实测锚定梯度只有 SFT 梯度的 **1.8%**。要让锚定项与 SFT 拉力同量级,
需要 λ* ≈ 1/0.018 ≈ **55**。达到 λ*=55 需要约 5.5×10⁶ 步——**是实际训练预算
2,470 步的 2,200 倍**。哪怕只要 λ=1,也需要 10⁵ 步(预算的 40 倍)。

结论:在这组超参数下,anchor 在整个训练周期内的实际强度约为所需强度的
**0.05%,failure 是超参数代数上的必然**,与"锚定思想是否正确"无关。
feasibility 10/10 fail 不构成对机制的证伪,只构成对求解器排期的证实。

### 3.3 即使 λ 够大,当前 anchor 的边际价值也趋近于零

每个 batch 已有 112/128 专家数据——这本身是强 rehearsal。而 anchor replay set
的 16 个窗口取自**专家数据支撑集内**的轨迹,故锚定梯度方向与专家 rehearsal 梯度
近似共线:

```
∇d_g ≈ c · ∇L_expert|_{局部},  c > 0   (同一分布上"别偏离 θ₀"与"继续拟合专家"同向)
```

共线方向上再加一个小权重项,期望收益 ≈ 0。这精确解释了 anchor preservation
**+0.03、3/5 同向(符号检验 p=0.5,纯噪声水平)**。

**anchor 必须保护 rehearsal 保护不到的东西**:π₀ 实际部署时访问、但专家数据不覆盖
的状态(复合误差区、边界区)。锚定集应从 **π₀ 的 on-policy rollout 状态**(800 条
traced corpus 里现成)采样,而非专家帧。

### 3.4 锚定集大小:16 个窗口在数学上不可能近似期望约束

约束的对象是 E_{x∼A_g}[·]。用 M 个样本约束期望,估计误差 ~ σ/√M;更重要的是
在 16 个固定点上强制 d=0 是**函数插值**,不是**分布约束**——网络有足够容量在
16 个点上精确复现 θ₀ 同时在其余状态任意漂移。要让"点约束 ⇒ 分布约束"的泛化差距
小于 ε 的行为等效量,锚定集需要**数百个独立窗口**(按 episode 聚类抽样),
并且必须留出**不进训练的 held-out probe 分割**来测真实的 d_g——
在训练用的 16 个点上测 d_g 只测出了记忆化程度。

### 3.5 求解器的替换:把渐进纠偏换成有每步保证的投影

dual ascent 的数学特征是"先违反、后拉回",与"全程不破坏旧能力"的目标错配。
A-GEM 式投影有逐步保证:

```
g' = g − min(0, ⟨g, g_ref⟩) / ‖g_ref‖² · g_ref   ⟹  ⟨g', g_ref⟩ ≥ 0
```

即每一步更新在一阶意义下不增加锚定损失,不需要 λ 的百万步爬坡。若保留拉格朗日路线,
则必须:增广项 ρ>0、λ₀ 由梯度比先验直接初始化(λ₀ ≈ 梯度比⁻¹ × 目标强度,
量级 10–50)、η_λ 按 (d−ε) 的实际尺度归一。两条路线都要过 §6 的阳性对照才准上场。

### 3.6 ε 的二次标度律(可检验)

d_g 在 θ₀ 处一阶导为零(θ₀ 是全局最小),故局部为二次型:

```
d_g(θ₀ + Δθ) ≈ Δθᵀ H Δθ  ⟹  沿插值路径 θ(α) = θ₀ + α(θ_SFT − θ₀):
d_g(α) ≈ α² · d_g(1)
```

行为退化(成功率降)在小漂移区同样近似二次 ⟹ **d_g 与行为遗忘在局部近似线性相关**。
这给出 ε 的校准程序:在 α ∈ {0, 0.1, 0.25, 0.5, 0.75, 1} 网格上同时测 d_g(α) 与
行为保持率,验证 α² 律,读出"行为掉 2%"对应的 d_g 值作为 ε_v2。
当前的 ε=10⁻⁴ 是 identity 冒烟容差,没有任何行为语义。
同时必须统一 raw/EMA:约束、probe、部署三处用同一权重族,step-0 先报 raw–EMA 基线差。

---

## 4. 推演 IV:测量端的均值回归是入组规则的直接后果

入组条件"2/3 成功"+ 保持条件"3/3 成功"意味着:即使参数完全不变,
表观保持率 = E[p³ | 入组] < 1。设状态真实成功率 p 有分布,入组按 观测≥2/3 截断,
则 base fresh 自身的"遗忘率"

```
1 − E[p³ | obs ≥ 2/3] > 0
```

实测正是 7/60 = 11.7%。所以 3/3 门的读数里混着一个 ~12% 的纯噪声地板,
任何方法差异都要在这个地板上叠加,信噪比极差。

**v2 端点**:成对成功概率差 Δ_pres = mean_s[p̂_θ(s) − p̂_{θ₀}(s)],
新旧策略共用 env seed + policy seed(公共随机数)。CRN 配对消掉状态难度方差,
每状态重复 R≥5。配对方差 ~ 2p(1−p)(1−ρ_corr)/R,在 ρ_corr≈0.5 时等效样本量翻倍。

**seed 数**:符号检验功效表(精确值):4/5 p=0.1875;5/5 p=0.0312;7/8 p=0.0352;
8/10 p=0.0547;9/10 p=0.0107。确证轮要么 **≥8 个训练 seed**,要么用配对 t/
Wilcoxon 于连续端点 Δ_pres。5 seeds 只够开发筛选。

---

## 5. 可检验预测(先花一天验证理论,再花两周跑实验)

理论如果是对的,以下五条必须成立;任何一条不成立,对应章节的设计要改。

- **P1(残差预言,CPU/单卡即可)**:对 10 个已导出 chunk 计算
  ‖a_target − f_{θ₀}(s)‖²(共享噪声/时间步),B1 的均值显著大于 N1。
  这是 §2.2 的直接检验,也是 B1 五连败的最便宜确认。
- **P2(干扰预言)**:B1 各 seed 的最终权重漂移 ‖θ − θ₀‖ 与其 preservation/adaptation
  退化幅度正相关,且 B1 的漂移 > N1 的漂移。
- **P3(时间尺度预言)**:B2/B3 训练日志里 λ 的终值 ≈ 0.02–0.03(§3.1 的算术),
  且锚定项从未达到 SFT 拉力的 1%。
- **P4(二次律预言)**:插值实验中 d_g(α)/d_g(1) 对 α² 的拟合 R² > 0.95。
- **P5(共线预言)**:在 anchor replay 窗口上,∇d_g 与 ∇L_expert 的余弦相似度显著为正
  (解释 anchor 无边际收益)。

---

## 6. BRACE v2 实验设计(每个参数标注来源章节)

### E0 — 理论取证(1 天,不占多卡)
P1/P2/P3/P5 的四项计算 + 9 个旧 branch 点按配对聚类推断重算(预期 5 accepted → 2)。
旧 5 chunk 全部降级为 pilot evidence。

### E1 — 重验证(主实验,~2 天 8 卡)
- 点源:32 个 mixed seeds × 3 点/seed = 96 点;分支点三类**按定义实现**
  (分歧峰值 / 首次持续分歧 / 用 rng 真随机采样的负对照)。
- 对照:**同状态重采样**——冻结 π₀ 在快照状态换噪声采 K 个动作片段(§1.1)。
- 每点 candidate 8 次 vs control 8 次,公共续跑种子配对(§1.2)。
- 判定:配对 Fisher/随机化检验 + BH(FDR=0.1),seed 级聚类 bootstrap 报告区间;
  负对照点必须整体不显著,否则判流程污染,全批作废。
- 完整性 gate(≥10 seed / ≥30 点 / 单 seed ≤0.2)做成硬 NO-GO,不满足直接拒绝导出。
- 预算:96×16 = 1,536 条续跑。

### E1b — 全失败 seed 的 best-of-K 抢救(自我提升的真正增量来源)
16 个全失败 seed 是改进空间最大的地方。在失败轨迹的关键状态上从 π₀ 重采 K=8:
成功概率从 p 放大到 1−(1−p)^K(p=0.1 时 K=8 → 57%)。在这些 seed 上
control 基线 ≈ 0,任何验证出的成功 chunk 都是纯增量。这一步把 BRACE 从
"复述已会的"升级为"用自己的方差探索不会的"——这才是 self-improvement 的数学本体。

### E2 — 数据构造(§2.5)
导出整条验证续跑(目标 M ≥ 6,000 帧);**w = ρ/q ≤ 3 写进协议做硬约束**;
训练前打印 (M, q, ρ, w) 四元组,w>3 拒绝启动。

### E3 — 锚定 v2(§3)
- 锚定集:从 800 条 traced corpus 的 on-policy 状态按 episode 聚类采样
  **300–500 个窗口**,训练/probe 分割,probe 永不进训练。
- 求解器:主选 **A-GEM 投影**(每步保证);对照臂 增广拉格朗日
  (ρ>0,λ₀≈10–50,η_λ 归一化)。修掉 `lambda_init` 未生效、`lambda_max` 未传入。
- ε:由 E4 插值校准给出行为语义(§3.6),废除 10⁻⁴。
- raw/EMA 统一,step-0 报基线差。

### E4 — 阳性对照 + 插值校准(半天,先于 E3 的任何行为评估)
- 阳性对照:从 θ₀ 做 50 步纯 SFT 制造漂移,停新数据只跑稳定机制,看 d_g 能否拉回。
  拉不回 = 求解器问题,禁止进入行为评估。
- 插值:α 网格测 d_g 与行为,验证 P4,产出 ε_v2。

### E5 — 确证因子实验(全部 gate 通过后)
- 臂:U0 / N1' / B1' / B2' / B3'(N1'/B1' 用 E1 重验证数据,B2'/B3' 用 E3 机制),
  外加**普通旧数据 rehearsal 加强臂**作为稳定机制的强基线(§3.3)。
- 训练 seed:**8 个**(§4)。
- 端点:CRN 配对 Δ_pres(preservation)与 easy/hard 适应率;预注册两个主检验:
  ① B1'−N1' > 0(验证有效);② 稳定机制 − rehearsal > 0(锚定有效);
  只有两者分别成立才解释 B3' 交互。
- 判定矩阵沿用:最终 d_g × 行为保持 四象限,只有双通过才冻结方法。

### 时间线与算力(8 卡)
E0 1 天(CPU/单卡)→ E1+E1b 2 天 → E4 半天 → E2/E3 准备 1 天 →
E5 训练 40 条(5 臂×8 seed,10 epoch 短训)约 2 天 + 评估 2 天。
关键排序原则:**任何训练启动之前,对应的预测(P1–P5)必须先被验证**。

---

## 7. 一段话总结

BRACE 的两个思想——用同状态反事实验证解决信用分配、用锚定约束解决灾难性干扰——
在数学上是自洽的,而且 §2.3 表明它们互为表里:验证选出的高价值 chunk 恰恰是
高方差、高残差、高干扰的样本,所以**越有效的验证越需要有效的干扰控制**。
pilot 的失败全部来自实现参数与数学要求相差数量级:对照混淆项被选点规则主动放大、
K=3 无功效、w=99 的记忆化、λ 差 2,200 倍时间尺度、16 点插值冒充分布约束、
ε 无行为语义、入组规则自带 12% 噪声地板。v2 的每个参数(K=8、w≤3、λ₀≈10–50 或
A-GEM、锚定集 300–500 on-policy 窗口、8 seeds、CRN 配对端点)都是从对应推演解出来的,
不再有拍脑袋常数。
